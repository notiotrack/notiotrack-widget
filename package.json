{
  "name": "api-notiotrack",
  "version": "1.0.0",
  "description": "Single-file bundled library for api-notiotrack",
  "type": "module",
  "main": "build/script.js",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.4.4"
  }
}
